import 'package:flutter/material.dart';

class Asccesories extends StatelessWidget {
  const Asccesories({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('accesories'),
      ),
    );
  }
}
